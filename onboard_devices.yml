---
# PLAY 1: Forbered IP'er
- name: Forbered de ukendte IP-adresser
  hosts: localhost
  gather_facts: no
  tasks:
    - name: Tilføj fundne IP'er til midlertidig gruppe
      ansible.builtin.add_host:
        name: "{{ item }}"
        groups: discovered_routers
        ansible_connection: network_cli
        ansible_network_os: ios
        ansible_user: "{{ onboard_user }}"
        ansible_password: "{{ onboard_password }}"
      loop: "{{ target_ips.split(',') }}"
      when: target_ips is defined

# PLAY 2: Deep Discovery (Read-only)
- name: Brownfield Onboarding (VLAN Logic Mode)
  hosts: discovered_routers
  gather_facts: no

  vars:
    netbox_url: "{{ lookup('ansible.builtin.env', 'NETBOX_URL') }}"
    netbox_token: "{{ lookup('ansible.builtin.env', 'NETBOX_TOKEN') }}"

  tasks:
    - name: 1. Hent informationer (Show vlan / Show version) 🔍
      cisco.ios.ios_facts:
        gather_subset: 
          - hardware
          - interfaces
          - config

    - name: 2. Basis Opsætning i NetBox
      block:
        - netbox.netbox.netbox_manufacturer:
            netbox_url: "{{ netbox_url }}"
            netbox_token: "{{ netbox_token }}"
            validate_certs: no
            data: { name: "Cisco" }
        - netbox.netbox.netbox_vlan_group:
            netbox_url: "{{ netbox_url }}"
            netbox_token: "{{ netbox_token }}"
            validate_certs: no
            data:
              name: "{{ target_site | upper }}-VLANS"
              slug: "{{ target_site | lower }}-vlans"
              scope_type: "dcim.site"
              scope: { name: "{{ target_site | upper }}" }
        - netbox.netbox.netbox_device_type:
            netbox_url: "{{ netbox_url }}"
            netbox_token: "{{ netbox_token }}"
            validate_certs: no
            data:
              manufacturer: "Cisco"
              model: "{{ ansible_net_model | upper }}"
              slug: "{{ ansible_net_model | lower | replace(' ', '-') }}"
      delegate_to: localhost
      run_once: true

    - name: 3. Opret/Opdater Switchen
      netbox.netbox.netbox_device:
        netbox_url: "{{ netbox_url }}"
        netbox_token: "{{ netbox_token }}"
        validate_certs: no
        data:
          name: "{{ ansible_net_hostname }}"
          device_type: "{{ ansible_net_model | upper }}"
          device_role: "Router"
          site: "{{ target_site }}"
          serial: "{{ ansible_net_serialnum }}"
          status: "active"
        state: present
      delegate_to: localhost

    - name: 4. SYNC VLANs (Fra switch-listen) 🏷️
      netbox.netbox.netbox_vlan:
        netbox_url: "{{ netbox_url }}"
        netbox_token: "{{ netbox_token }}"
        validate_certs: no
        data:
          name: "{{ item.name }}"
          vid: "{{ item.vlan_id }}"
          site: "{{ target_site }}"
          group: "{{ target_site | upper }}-VLANS"
          status: "active"
        state: present
      loop: "{{ ansible_net_vlans | default([]) }}"
      # Vi tager alt fra VLAN 2 til 999
      when: item.vlan_id | int >= 2 and item.vlan_id | int < 1000
      delegate_to: localhost

    - name: 5. SYNC PREFIXES (Matematik: 10.50.[VLAN-300].0/24) 🌐
      netbox.netbox.netbox_prefix:
        netbox_url: "{{ netbox_url }}"
        netbox_token: "{{ netbox_token }}"
        validate_certs: no
        data:
          # Her bruger vi en super-simpel beregning for at undgå YAML-fejl
          prefix: "10.50.{{ item.vlan_id | int - 300 }}.0/24"
          site: "{{ target_site }}"
          status: "active"
          vlan: 
            vid: "{{ item.vlan_id }}"
            group: "{{ target_site | upper }}-VLANS"
        state: present
      loop: "{{ ansible_net_vlans | default([]) }}"
      # Kun VLANs fra 300 og op, som du bad om
      when: item.vlan_id | int >= 300 and item.vlan_id | int < 1000
      delegate_to: localhost

    - name: 6. SYNC INTERFACES 🔌
      netbox.netbox.netbox_device_interface:
        netbox_url: "{{ netbox_url }}"
        netbox_token: "{{ netbox_token }}"
        validate_certs: no
        data:
          device: "{{ ansible_net_hostname }}"
          name: "{{ item.key }}"
          type: "1000base-t"
          enabled: "{{ item.value.enabled | default(true) }}"
        state: present
      loop: "{{ ansible_net_interfaces | dict2items }}"
      delegate_to: localhost
