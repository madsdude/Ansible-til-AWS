---
# PLAY 1: Forbered FortiGate IP'en
- name: "Forbered FortiGate IP"
  hosts: localhost
  gather_facts: no
  tasks:
    - name: "Tilføj firewall IP til midlertidig gruppe"
      ansible.builtin.add_host:
        name: "{{ item }}"
        groups: discovered_firewalls
      loop: "{{ target_ips.split(',') }}"
      when: target_ips is defined

# PLAY 2: REST API Discovery & NetBox Onboarding
- name: "FortiGate HA Onboarding (REST API)"
  hosts: discovered_firewalls
  gather_facts: no

  vars:
    netbox_url: "{{ lookup('ansible.builtin.env', 'NETBOX_URL') }}"
    netbox_token: "{{ lookup('ansible.builtin.env', 'NETBOX_TOKEN') }}"
    # Henter din nye token fra Semaphore Key Store
    fortigate_token: "{{ lookup('ansible.builtin.env', 'FORTIGATE_API_TOKEN') }}"

tasks:
    - name: "1. Hent System Status fra FortiGate (Master) 🕵️"
      ansible.builtin.uri:
        url: "https://{{ inventory_hostname }}/api/v2/monitor/system/status"
        method: GET
        headers:
          Authorization: "Bearer {{ fortigate_token }}"
        validate_certs: no
        return_content: yes
      register: fg_status

    - name: "1b. Udled Site fra Firewallens navn (TST-FW01 -> TST) 🧠"
      set_fact:
        # Vi splitter navnet ved '-' og tager den første del (indeks 0). Vi sikrer også at det er med STORE bogstaver.
        fw_site: "{{ fg_status.json.results.hostname.split('-')[0] | upper }}"
      when: fg_status.json.results.hostname is defined

    - name: "2. Hent HA Status (Cluster Info) 👯‍♂️"
      ansible.builtin.uri:
        url: "https://{{ inventory_hostname }}/api/v2/monitor/system/ha"
        method: GET
        headers:
          Authorization: "Bearer {{ fortigate_token }}"
        validate_certs: no
        return_content: yes
      register: fg_ha
      ignore_errors: yes

    - name: "3. Hent alle Interfaces & IP'er 🔌"
      ansible.builtin.uri:
        url: "https://{{ inventory_hostname }}/api/v2/monitor/system/interface"
        method: GET
        headers:
          Authorization: "Bearer {{ fortigate_token }}"
        validate_certs: no
        return_content: yes
      register: fg_interfaces

    - name: "4. Opret Basis Opsætning i NetBox (Fortinet & Firewall)"
      block:
        - netbox.netbox.netbox_manufacturer:
            netbox_url: "{{ netbox_url }}"
            netbox_token: "{{ netbox_token }}"
            validate_certs: no
            data: { name: "Fortinet", slug: "fortinet" }
        - netbox.netbox.netbox_device_role:
            netbox_url: "{{ netbox_url }}"
            netbox_token: "{{ netbox_token }}"
            validate_certs: no
            data: { name: "Firewall", color: "ff9800" }
        - netbox.netbox.netbox_device_type:
            netbox_url: "{{ netbox_url }}"
            netbox_token: "{{ netbox_token }}"
            validate_certs: no
            data:
              manufacturer: "Fortinet"
              model: "FortiGate HA Cluster"
              slug: "fortigate-ha-cluster"
      delegate_to: localhost
      run_once: true

    - name: "5. Opret den Aktive FortiGate (Master) i NetBox 🏆"
      netbox.netbox.netbox_device:
        netbox_url: "{{ netbox_url }}"
        netbox_token: "{{ netbox_token }}"
        validate_certs: no
        data:
          name: "{{ fg_status.json.results.hostname | default('FortiGate-Master') }}"
          device_type: "FortiGate HA Cluster"
          device_role: "Firewall"
          # HER ER MAGIEN: Vi bruger vores nye udledte site!
          site: "{{ fw_site }}" 
          serial: "{{ fg_status.json.results.serial | default('') }}"
          status: "active"
      delegate_to: localhost
    - name: "6. SYNC Interfaces til Firewallen 🔌"
      netbox.netbox.netbox_device_interface:
        netbox_url: "{{ netbox_url }}"
        netbox_token: "{{ netbox_token }}"
        validate_certs: no
        data:
          device: "{{ fg_status.json.results.hostname | default('FortiGate-Master') }}"
          name: "{{ item.name }}"
          type: "1000base-t"
          enabled: "{{ item.status == 'up' }}"
          mac_address: "{{ item.mac | default(omit) }}"
      loop: "{{ fg_interfaces.json.results | default([]) }}"
      when: item.name is defined
      delegate_to: localhost
      ignore_errors: yes

    - name: "7. SYNC IP Adresser (Gateways) 🌐"
      netbox.netbox.netbox_ip_address:
        netbox_url: "{{ netbox_url }}"
        netbox_token: "{{ netbox_token }}"
        validate_certs: no
        data:
          # FortiGate returnerer normalt IP og maske separat. Vi samler dem!
          address: "{{ item.ip }}/{{ item.mask | default('255.255.255.0') | ipaddr('prefix') }}"
          status: "active"
          assigned_object:
            device: "{{ fg_status.json.results.hostname | default('FortiGate-Master') }}"
            name: "{{ item.name }}"
      loop: "{{ fg_interfaces.json.results | default([]) }}"
      when: 
        - item.ip is defined
        - item.ip != "0.0.0.0" # Spring interfaces uden IP over
      delegate_to: localhost
      ignore_errors: yes
